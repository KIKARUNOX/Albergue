{
  "name": "codigo316-firebase-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "npx firebase emulators:start --only functions",
    "deploy": "npm run build && npx firebase deploy --only functions",
    "logs": "npx firebase functions:log"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "firebase-tools": "^14.16.0",
    "typescript": "^5.9.3"
  }
}
